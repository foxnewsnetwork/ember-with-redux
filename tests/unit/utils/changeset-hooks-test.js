/* jshint expr:true */
import { expect } from 'chai';
import {
  describe,
  it
} from 'mocha';
import changesetHooks from 'ember-with-redux/utils/changeset-hooks';

describe('changesetHooks', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = changesetHooks();
    expect(result).to.be.ok;
  });
});
