/* jshint expr:true */
import { expect } from 'chai';
import {
  describe,
  it
} from 'mocha';
import mergeWith from 'ember-with-redux/utils/merge-with';

describe('mergeWith', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = mergeWith();
    expect(result).to.be.ok;
  });
});
