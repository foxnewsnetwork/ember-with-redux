/* jshint expr:true */
import { expect } from 'chai';
import {
  describe,
  it
} from 'mocha';
import dsCollections from 'ember-with-redux/utils/ds-collections';

describe('dsCollections', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = dsCollections();
    expect(result).to.be.ok;
  });
});
